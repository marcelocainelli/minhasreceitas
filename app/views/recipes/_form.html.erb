<div class="row">
    <div class='col-md-8 col-md-offset-2 well'>
        <%= form_for(@recipe, html: { multipart: true }) do |f| %>
            <div class="form-group">
              Nome da Receita : <%= f.text_field :name, class:"form-control" %><br />
            </div>
            
            <div class = "form-group">
              Sobre a Receita : <%= f.text_field :summary, class:"form-control" %><br />
            </div>
            
            <div class = "form-group">
              Descrição : <%= f.text_area :description, :rows => "8", class:"form-control" %><br />
            </div>
             <div class = "form-group">
           Estilos (Marque todos que se aplicam):<br/>
            
            <%= f.collection_check_boxes :style_ids, Style.all, :id, :name do |cb| %>
              <% cb.label(class: "checkbox-inline") { cb.check_box(class: "checkbox") + cb.text } %>
            <% end %>
            </div>
             <div class = "form-group">
            Ingredientes (Marque todos que se aplicam):<br/>
            
            <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name do |cb| %>
              <% cb.label(class: "checkbox-inline") { cb.check_box(class: "checkbox") + cb.text } %>
            <% end %>
            </div>
            
            
              Upload Imagem: <i>(opcional)</i>
               <%= f.file_field :picture, accept:'image/jpeg,image/gif,image/png' %><br />
  
              <%= f.submit @recipe.new_record? ? "Criar Nova Receita" : "Atualizar Receita", class:"btn btn-warning" %>
              <%= link_to "Cancel", :back, class:"btn btn-danger" %>
        <% end %>
    </div>
</div>